Crates in use:

- `clap` — CLI
- `serde` / `serde_json` — JSON output
- `similar` — observational line counts / summaries only (Codex apply-patch does the same)
- custom exact locator + cursor emit — V4A apply engine
- `tempfile` — exclusive same-directory temps
- `blake3` — content fingerprints (`blake3` in JSON)
- `camino` or strict `PathBuf` helpers — repo-relative paths
- `proptest` — parser / path properties
- `assert_cmd` / `predicates` — CLI tests
- `criterion` — `benches/apply_update.rs`
- `insta` — optional diagnostic snapshots
- `thiserror` — optional internals; public errors stay `PublicError` / `ErrorCode`
- `libfuzzer-sys` — via `fuzz/` workspace (`cargo-fuzz`), not the main crate

Not on the V4A apply path:

- `diffy` — unified-diff parse/format/display (Codex TUI, Zed ACP UI)
- `flickzeug` — `diffy` fork with `FuzzyConfig` / unified `apply` (rattler-build, cargo-mutants); still not V4A
- `fs2` — unused; concurrency = blake3 revalidation
